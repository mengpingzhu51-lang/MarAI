{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "User profile document representing customized application preferences and role flags.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "email": {
          "type": "string"
        },
        "deptAndTitle": {
          "type": "string"
        },
        "bio": {
          "type": "string"
        },
        "avatarUrl": {
          "type": "string"
        }
      },
      "required": [
        "email"
      ]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Store authenticating operational preferences and profile details"
    }
  }
}
